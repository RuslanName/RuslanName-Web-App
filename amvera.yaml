meta:
  environment: jvm
  toolchain:
    name: maven
    version: 17

build:
  dockerfile: Dockerfile
  args: -Dserver.port=80 -Pproduction

run:
  jarName: TelegramWebApp.jar
  persistenceMount: /data
  containerPort: 80
